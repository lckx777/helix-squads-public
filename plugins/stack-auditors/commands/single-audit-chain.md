---
description: Fastest path — sa-council routes to single mentor, mentor audits with skill, returns verdict
---

# single-audit-chain

Fastest path — sa-council routes to single mentor, mentor audits with skill, returns verdict

## Execution

This command triggers the `single-audit-chain` chain. The canonical chain
definition lives at `chains/single-audit-chain.yaml` within this plugin.

See the YAML file for detailed step sequence. Execute each step by invoking the specified agent and task in order, passing outputs forward as inputs to subsequent steps.

## Arguments

$ARGUMENTS will be passed as the initial input to step 1.
